import defaultTheme from "tailwindcss/defaultTheme";
import forms from "@tailwindcss/forms";

/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./vendor/laravel/framework/src/Illuminate/Pagination/resources/views/*.blade.php",
        "./storage/framework/views/*.php",
        "./resources/views/**/*.blade.php",
        "./resources/**/*.blade.php", // Ditambahkan untuk mencakup semua file blade
        "./resources/**/*.js", // Ditambahkan untuk mencakup semua file JavaScript
        "./resources/**/*.vue", // Ditambahkan untuk mencakup semua file Vue jika Anda menggunakannya
    ],

    theme: {
        extend: {
            fontFamily: {
                sans: ["Figtree", ...defaultTheme.fontFamily.sans],
            },
        },
    },

    plugins: [forms],
};
